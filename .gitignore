node_modules/
dist/
build/
*.log
npm-debug.log*
*.tsbuildinfo

# Env files (keep the example)
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json

# Coverage / misc
coverage/
.tmp/

# Local agent tooling config, not relevant to other contributors
.claude/

# Real photos tied to this site's identity (e.g. the Dog House storefront
# background) — this repo is public, and we don't necessarily hold
# redistribution rights, so these stay local/server-only. See
# packages/frontend/public/branding/README.md.
packages/frontend/public/branding/*
!packages/frontend/public/branding/README.md

